---
  # main playbook that calls others (install_tools, install_apache_playbook, install_appserver_playbook)
  - hosts: all
    become: yes

    roles:
      - geerlingguy.ntp
      - geerlingguy.git
      - geerlingguy.java
      - geerlingguy.jenkins
      - geerlingguy.repo-epel
      - geerlingguy.ansible
      - webserver1
      - appserver1